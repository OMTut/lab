apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageRepository
metadata:
  name: admirals-log
  namespace: flux-system
spec:
  image: ghcr.io/omtut/admirals-log
  interval: 5m
---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: admirals-log
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: admirals-log
  filterTags:
    pattern: '^build-(\d+)'
    extract: '$1'
  policy:
    numerical:
      order: asc
---
apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImageUpdateAutomation
metadata:
  name: admirals-log
  namespace: flux-system
spec:
  interval: 1m
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  git:
    checkout:
      ref:
        branch: main
    commit:
      author:
        email: fluxcdbot@users.noreply.github.com
        name: fluxcdbot
      messageTemplate: |
        chore: update admirals-log image
        {{range .Updated.Images}}
        - {{println .}}{{end}}
    push:
      branch: main
  update:
    path: ./apps/base/admirals-log
    strategy: Setters
