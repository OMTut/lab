apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: mealie-staging-cert
  namespace: mealie
spec:
  secretName: mealie-staging-tls
  issuerRef:
    name: letsencrypt-staging
    kind: ClusterIssuer
  dnsNames:
    - mealie.hondo-dev.com