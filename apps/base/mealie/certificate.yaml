apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: mealie-cert-public
  namespace: mealie
spec:
  secretName: mealie-public-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - mealie.hondo-dev.com